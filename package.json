{
    "private": true,
    "workspaces": [
        "packages/*",
        "examples/*"
    ],
    "scripts": {
        "lerna": "lerna",
        "build": "lerna run build",
        "test": "lerna run test",
        "pub:release": "lerna --scope=fancy-discord.js exec npm publish"
    },
    "devDependencies": {
        "lerna": "^4.0.0"
    }
}
